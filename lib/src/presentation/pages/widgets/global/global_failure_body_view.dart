import 'package:flutter/material.dart';

class GlobalFailureBodyView extends StatelessWidget {
  const GlobalFailureBodyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('잠시 후 다시 시도해주세요'),
    );
  }
}
